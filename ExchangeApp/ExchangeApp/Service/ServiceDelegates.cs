using System.IO.MemoryMappedFiles;

namespace ExchangeApp.Model.Implementations
{
    public class ServiceDelegates
    {
            public delegate void UpdateOperations();

            public delegate void UpdateCurrencies();
    }
}